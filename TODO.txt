complete commands and flags, add frirnedlier cli (levennstein metirc etc..)
commands to add:
    copy
    reset / destroy
    search
    export

show single passwrod without opening teh entire vault

fix the io to eccho commands? to not deltet the local output but deletre the global. replace secret with safevar

refactor safevar

improve the parser

make the vault module itself safe and check for its arguments ("password does not exists in vault")

fix the directory for encapsulation

fix headers and soidum abstraction

add colors to terminal

add reqs for master password

catch signals

add timeouts (threads)

chagne the decrypto and encrypto back to normal and cahggne their argument to get a safevar

review passing by value & and && and secrets lifetime